# Models

The models package provides a unified backend wrapper for AI providers like Google, OpenAI, and Anthropic, allowing client applications to run bidirectional text streaming sessions using Go channels.

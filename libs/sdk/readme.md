# Provar SDK

The Provar SDK represents the core programming library for the Provar end-to-end testing tool, combining domains/types, the execution and compilation engine, and the bidirectional streaming model wrappers to be consumed by client applications.

/**
 * APP_NAME is the user-facing application name for the Provar Editor.
 */
export const APP_NAME = "Provar Editor";

/**
 * DEFAULT_WIDTH is the default window width of the editor.
 */
export const DEFAULT_WIDTH = 1200;

/**
 * DEFAULT_HEIGHT is the default window height of the editor.
 */
export const DEFAULT_HEIGHT = 800;

/**
 * TEST_FILE_EXTENSION is the filename extension used for Provar test definitions.
 */
export const TEST_FILE_EXTENSION = ".test.yml";

/**
 * COLORS defines the editor theme color tokens.
 */
export const COLORS = {
  primary: "#6366f1", // indigo-500
  bg: "#0e1116",
  sidebar: "#161b22",
  border: "#30363d",
} as const;

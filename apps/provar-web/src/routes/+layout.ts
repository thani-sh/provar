// Static-site generation: prerender every route.
export const prerender = true;
// No client-side router needed for a static marketing site — output is fully static HTML.
export const ssr = true;
export const trailingSlash = "never";

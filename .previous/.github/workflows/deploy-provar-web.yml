name: Deploy provar-web

on:
  push:
    branches: [main]
    paths:
      - "apps/provar-web/**"
      - "libs/**"
  workflow_dispatch:

concurrency:
  group: deploy-provar-web
  cancel-in-progress: true

jobs:
  build:
    name: Build static site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Build provar-web
        run: bun run --filter '@apps/provar-web' build
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: provar-web-build
          path: apps/provar-web/build
          retention-days: 1
          if-no-files-found: error

  deploy:
    name: Deploy to Cloudflare
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: prod
      url: https://provar.se
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: provar-web-build
          path: apps/provar-web/build
      - name: Deploy provar-web
        uses: cloudflare/pages-action@v1
        with:
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          projectName: provar-se
          directory: apps/provar-web/build
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
